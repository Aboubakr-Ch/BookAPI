using Microsoft.EntityFrameworkCore;

namespace BookAPI
{
    public class SharedBll
    {
       // var options = new DbContextOptionsBuilder<BookContext>().UseInMemoryDatabase(databaseName: "Test").Options;
    }
}
